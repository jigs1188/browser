package dev.mer.ui.theme

import androidx.compose.ui.graphics.Color

// Primary palette — deep indigo to electric blue gradient feel
val Primary = Color(0xFF6366F1)        // Indigo-500
val PrimaryDark = Color(0xFF4F46E5)    // Indigo-600
val PrimaryLight = Color(0xFF818CF8)   // Indigo-400

// Secondary — teal accent
val Secondary = Color(0xFF14B8A6)      // Teal-500
val SecondaryDark = Color(0xFF0D9488)  // Teal-600

// Surface colors — dark mode first
val SurfaceDark = Color(0xFF0F172A)         // Slate-900
val SurfaceDarkElevated = Color(0xFF1E293B) // Slate-800
val SurfaceCard = Color(0xFF334155)         // Slate-700

// Text
val TextPrimary = Color(0xFFF1F5F9)    // Slate-100
val TextSecondary = Color(0xFF94A3B8)  // Slate-400
val TextMuted = Color(0xFF64748B)      // Slate-500

// Status
val Success = Color(0xFF22C55E)
val Warning = Color(0xFFF59E0B)
val Error = Color(0xFFEF4444)

// Url bar
val UrlBarBackground = Color(0xFF1E293B)
val UrlBarText = Color(0xFFE2E8F0)
